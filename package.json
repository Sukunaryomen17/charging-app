{
  "name": "charging-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && npm start"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
